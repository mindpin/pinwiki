.p
  = render 'wiki/parts/nav'

- @history.each do |version|
  %p
    = version.version
    
    = version.creator.name
    
    = version.action
    
    = version.audited_changes

    = version.created_at
